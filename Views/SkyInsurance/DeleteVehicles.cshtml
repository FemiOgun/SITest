@model IEnumerable<SkyInsuranceThird.Models.Vehicle>

@{
    ViewBag.Title = "DeleteVehicles";
}

<h2>Delete Vehicles</h2>

<h3>Do you want to delete Vehicles?</h3>
<div>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MakeModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Registration)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MakeModel)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Registration)
                </td>
                <td></td>
            </tr>
        }

    </table>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
    @*<h4>Vehicle</h4>
        <hr />
        <dl class="dl-horizontal">

            <dd>
                @Html.HiddenFor(model => model.UserId)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Registration)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Registration)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.MakeModel)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.MakeModel)
            </dd>

        </dl>

        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }*@
</div>
